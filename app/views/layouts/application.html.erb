<!DOCTYPE html>
<html>
  <head>
    <title>Instagramclone</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
    <ul>
      <li><a href="#">Instagram Clone</a></li>
    </ul>
    </header>
    <br>
    <% flash.each do |key, value| %>
      <%= content_tag(:div, value, class: "#{key}") %>
    <% end %>
    <% if logged_in? %>
      <%= link_to "Profile", user_path(current_user.id), class: "btn btn-primary" %>
      <%= link_to "Logout", session_path(current_user.id), method: :delete, class: "btn btn-danger" %>
    <% else %>
      <%= link_to "Sign up", new_user_path %>
      <%= link_to "Login", new_session_path %>
    <% end %>
    <!-- <div class="content"> -->
      <%= yield %>
    <!-- </div> -->

    <footer>
      <p>運営会社Instagram株式会社</p>
      <p>© 2015-2016 Instagram Corp.</p>
    </footer>
  </body>
</html>
